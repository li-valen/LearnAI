function getStartDate() {
    // Get the input element
    var datePicker = document.getElementById("datePicker-start");

    // Get the selected date value
    var selectedDate = datePicker.value;

    // Display the selected date (you can customize this part based on your needs)
    alert("Selected Date: " + selectedDate);
}

function getEndDate() {
    // Get the input element
    var datePicker = document.getElementById("datePicker-end");

    // Get the selected date value
    var selectedDate = datePicker.value;

    // Display the selected date (you can customize this part based on your needs)
    alert("Selected Date: " + selectedDate);
}